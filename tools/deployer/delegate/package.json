{
  "name": "delegate",
  "version": "0.0.1",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ airswap/delegate",
    "create-local": "graph create --node http://localhost:8020/ airswap/delegate",
    "remove-local": "graph remove --node http://localhost:8020/ airswap/delegate",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 airswap/delegate"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.17.1",
    "@graphprotocol/graph-ts": "0.17.0"
  }
}
